# Zinc

Rust JavaScript engine from [florekio/Zinc](https://github.com/florekio/Zinc), with a bytecode VM and an experimental ARM64 JIT on macOS/aarch64.

This zoo integration builds the upstream `main` branch with `cargo build --release` and packages the resulting `zinc` CLI binary.
