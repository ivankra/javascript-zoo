# GocciaScript

FreePascal JavaScript engine from [frostney/GocciaScript](https://github.com/frostney/GocciaScript), packaged here as a zoo engine via its `ScriptLoader` CLI.

This integration builds the upstream `main` branch with `fpc` and exposes the resulting `build/ScriptLoader` binary through the standard zoo packaging flow.
