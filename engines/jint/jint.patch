diff --git a/Jint.Repl/Program.cs b/Jint.Repl/Program.cs
index c53c5c4d9..0763a2dd0 100644
--- a/Jint.Repl/Program.cs
+++ b/Jint.Repl/Program.cs
@@ -1,10 +1,11 @@
-﻿#nullable enable
+#nullable enable
 
 using System.Reflection;
 using Jint;
 using Jint.Native;
 using Jint.Native.Json;
 using Jint.Runtime;
+using Jint.Runtime.Modules;
 
 // ReSharper disable LocalizableElement
 
@@ -14,6 +15,8 @@ using Jint.Runtime;
 // Parse command line arguments
 string? inputFile = null;
 int? timeoutSeconds = null;
+bool runAsModule = false;
+bool exposeTest262 = false;
 
 for (int i = 0; i < args.Length; i++)
 {
@@ -41,6 +44,12 @@ for (int i = 0; i < args.Length; i++)
                 return 1;
             }
             break;
+        case "-m" or "--module":
+            runAsModule = true;
+            break;
+        case "--expose-test-262":
+            exposeTest262 = true;
+            break;
         case "-h" or "--help":
             PrintHelp();
             return 0;
@@ -61,15 +70,27 @@ var engine = new Engine(cfg =>
     {
         cfg.TimeoutInterval(TimeSpan.FromSeconds(timeoutSeconds.Value));
     }
+    if (runAsModule)
+    {
+        var basePath = inputFile != null
+            ? Path.GetDirectoryName(Path.GetFullPath(inputFile))!
+            : Directory.GetCurrentDirectory();
+        cfg.EnableModules(basePath, restrictToBasePath: false);
+    }
 });
 
 engine
-    .SetValue("print", new Action<object>(Console.WriteLine))
+    .SetValue("print", (Action<object?[]>) JsConsole.Print)
     .SetValue("console", new JsConsole())
     .SetValue("load", new Func<string, object>(
         path => engine.Evaluate(File.ReadAllText(path)))
     );
 
+if (exposeTest262)
+{
+    Test262Object.Install(engine);
+}
+
 // Execute file if provided via -f
 if (!string.IsNullOrEmpty(inputFile))
 {
@@ -81,18 +102,34 @@ if (!string.IsNullOrEmpty(inputFile))
 
     try
     {
-        var script = File.ReadAllText(inputFile);
-        var result = engine.Evaluate(script, inputFile);
-        if (!result.IsUndefined())
+        if (runAsModule)
+        {
+            var absolutePath = Path.GetFullPath(inputFile);
+            engine.Modules.Import(new Uri(absolutePath).AbsoluteUri);
+        }
+        else
         {
-            Console.WriteLine(result);
+            var script = File.ReadAllText(inputFile);
+            var result = engine.Evaluate(script, inputFile);
+            if (!result.IsUndefined())
+            {
+                Console.WriteLine(result);
+            }
         }
         return 0;
     }
     catch (JavaScriptException je)
     {
-        Console.Error.WriteLine($"Error: {je.Message}");
-        Console.Error.WriteLine(je.JavaScriptStackTrace);
+        Console.Error.WriteLine(FormatJavaScriptException(je));
+        if (je.JavaScriptStackTrace is { } stackTrace)
+        {
+            Console.Error.WriteLine(stackTrace);
+        }
+        return 1;
+    }
+    catch (ModuleResolutionException mre)
+    {
+        Console.Error.WriteLine($"Error: {mre.Message}");
         return 1;
     }
     catch (TimeoutException)
@@ -113,17 +150,33 @@ if (Console.IsInputRedirected)
     try
     {
         var script = Console.In.ReadToEnd();
-        var result = engine.Evaluate(script, "stdin");
-        if (!result.IsUndefined())
+        if (runAsModule)
         {
-            Console.WriteLine(result);
+            engine.Modules.Add("<stdin>", script);
+            engine.Modules.Import("<stdin>");
+        }
+        else
+        {
+            var result = engine.Evaluate(script, "stdin");
+            if (!result.IsUndefined())
+            {
+                Console.WriteLine(result);
+            }
         }
         return 0;
     }
     catch (JavaScriptException je)
     {
-        Console.Error.WriteLine($"Error: {je.Message}");
-        Console.Error.WriteLine(je.JavaScriptStackTrace);
+        Console.Error.WriteLine(FormatJavaScriptException(je));
+        if (je.JavaScriptStackTrace is { } stackTrace2)
+        {
+            Console.Error.WriteLine(stackTrace2);
+        }
+        return 1;
+    }
+    catch (ModuleResolutionException mre)
+    {
+        Console.Error.WriteLine($"Error: {mre.Message}");
         return 1;
     }
     catch (TimeoutException)
@@ -167,6 +220,10 @@ while (true)
     try
     {
         var result = engine.Evaluate(input, parsingOptions);
+        if (result.IsUndefined())
+        {
+            continue;
+        }
         JsValue str = result;
         if (!result.IsPrimitive() && result is not IJsPrimitive)
         {
@@ -185,7 +242,11 @@ while (true)
     catch (JavaScriptException je)
     {
         Console.ForegroundColor = ConsoleColor.Red;
-        Console.WriteLine(je.ToString());
+        Console.WriteLine(FormatJavaScriptException(je));
+        if (je.JavaScriptStackTrace is { } stackTrace3)
+        {
+            Console.WriteLine(stackTrace3);
+        }
     }
     catch (TimeoutException)
     {
@@ -199,6 +260,25 @@ while (true)
     }
 }
 
+static string FormatJavaScriptException(JavaScriptException je)
+{
+    string errorName;
+    if (je.Error.IsObject())
+    {
+        var nameVal = je.Error.AsObject().Get(new JsString("name"), je.Error);
+        errorName = nameVal.IsUndefined() ? "Error" : nameVal.ToString();
+    }
+    else
+    {
+        errorName = "Error";
+    }
+
+    var message = je.Message;
+    return string.IsNullOrEmpty(message)
+        ? $"Uncaught exception: {errorName}"
+        : $"Uncaught exception: {errorName}: {message}";
+}
+
 static void PrintHelp()
 {
     Console.WriteLine("Jint REPL - JavaScript interpreter");
@@ -207,12 +287,15 @@ static void PrintHelp()
     Console.WriteLine();
     Console.WriteLine("Options:");
     Console.WriteLine("  -f, --file <path>     Execute JavaScript file");
+    Console.WriteLine("  -m, --module          Run script as ES6 module");
+    Console.WriteLine("  --expose-test-262     Install the $262 test harness object");
     Console.WriteLine("  -t, --timeout <secs>  Set execution timeout in seconds");
     Console.WriteLine("  -h, --help            Show this help message");
     Console.WriteLine();
     Console.WriteLine("Examples:");
     Console.WriteLine("  jint                          Start interactive REPL");
     Console.WriteLine("  jint script.js                Execute script.js");
+    Console.WriteLine("  jint -m module.js             Execute module.js as ES6 module");
     Console.WriteLine("  jint -f script.js -t 10       Execute with 10 second timeout");
     Console.WriteLine("  echo \"1+1\" | jint             Execute from stdin");
     Console.WriteLine("  echo \"1+1\" | jint -t 5        Execute from stdin with timeout");
@@ -220,15 +303,17 @@ static void PrintHelp()
 
 file sealed class JsConsole
 {
-    public void Log(object value)
-    {
-        Console.WriteLine(value?.ToString() ?? "null");
-    }
+    public static string Format(object?[] values) =>
+        string.Join(" ", values.Select(v => v?.ToString() ?? "null"));
+
+    public static void Print(params object?[] values) => Console.WriteLine(Format(values));
+
+    public void Log(params object?[] values) => Console.WriteLine(Format(values));
 
-    public void Error(object value)
+    public void Error(params object?[] values)
     {
         Console.ForegroundColor = ConsoleColor.Red;
-        Console.WriteLine(value?.ToString() ?? "null");
+        Console.WriteLine(Format(values));
         Console.ResetColor();
     }
 }
diff --git a/Jint.Tests.Test262/Test262Test.cs b/Jint.Tests.Test262/Test262Test.cs
index 99425c04c..c52bf25a8 100644
--- a/Jint.Tests.Test262/Test262Test.cs
+++ b/Jint.Tests.Test262/Test262Test.cs
@@ -60,54 +60,11 @@ public abstract partial class Test262Test
             return JsValue.Undefined;
         }));
 
-        var o = engine.Realm.Intrinsics.Object.Construct(Arguments.Empty);
-        o.FastSetProperty("evalScript", new PropertyDescriptor(new ClrFunction(engine, "evalScript",
-            (_, args) =>
-            {
-                if (args.Length > 1)
-                {
-                    throw new Exception("only script parsing supported");
-                }
-
-                var script = Engine.PrepareScript(args.At(0).AsString(), options: new ScriptPreparationOptions
-                {
-                    ParsingOptions = ScriptParsingOptions.Default with { Tolerant = false },
-                });
-
-                return engine.Evaluate(script);
-            }), true, true, true));
-
-        o.FastSetProperty("createRealm", new PropertyDescriptor(new ClrFunction(engine, "createRealm",
-            (_, args) =>
-            {
-                var realm = engine._host.CreateRealm();
-                realm.GlobalObject.Set("global", realm.GlobalObject);
-                return realm.GlobalObject;
-            }), true, true, true));
-
-        o.FastSetProperty("detachArrayBuffer", new PropertyDescriptor(new ClrFunction(engine, "detachArrayBuffer",
-            (_, args) =>
-            {
-                var buffer = (JsArrayBuffer) args.At(0);
-                buffer.DetachArrayBuffer();
-                return JsValue.Undefined;
-            }), true, true, true));
-
-        o.FastSetProperty("gc", new PropertyDescriptor(new ClrFunction(engine, "gc",
-            (_, _) =>
-            {
-                GC.Collect();
-                GC.WaitForPendingFinalizers();
-                return JsValue.Undefined;
-            }), true, true, true));
-
-        o.FastSetProperty("IsHTMLDDA", new PropertyDescriptor(new IsHTMLDDA(engine), true, true, true));
+        var o = Test262Object.Install(engine);
 
         // Install agent support if needed
         agentManager?.InstallAgent(engine, o);
 
-        engine.SetValue("$262", o);
-
         foreach (var include in file.Includes)
         {
             engine.Execute(State.Sources[include]);
diff --git a/Jint/Native/JsValue.cs b/Jint/Native/JsValue.cs
index 669396d1c..2b585a4e2 100644
--- a/Jint/Native/JsValue.cs
+++ b/Jint/Native/JsValue.cs
@@ -252,7 +252,7 @@ public abstract partial class JsValue : IEquatable<JsValue>
     {
         if (value is null)
         {
-            return Null;
+            return type == typeof(void) ? Undefined : Null;
         }
 
         if (value is JsValue jsValue)
diff --git a/Jint/Runtime/Interop/DelegateWrapper.cs b/Jint/Runtime/Interop/DelegateWrapper.cs
index 088775646..8d9e7437b 100644
--- a/Jint/Runtime/Interop/DelegateWrapper.cs
+++ b/Jint/Runtime/Interop/DelegateWrapper.cs
@@ -136,6 +136,10 @@ internal sealed class DelegateWrapper : Function
             var result = _d.DynamicInvoke(parameters);
             if (!IsAwaitable(result))
             {
+                if (_d.Method.ReturnType == typeof(void))
+                {
+                    return JsValue.Undefined;
+                }
                 return FromObject(Engine, result);
             }
             return ConvertAwaitableToPromise(Engine, result!);
diff --git a/Jint/Test262Object.cs b/Jint/Test262Object.cs
new file mode 100644
index 000000000..f983d5384
--- /dev/null
+++ b/Jint/Test262Object.cs
@@ -0,0 +1,68 @@
+using Jint.Native;
+using Jint.Native.Object;
+using Jint.Runtime;
+using Jint.Runtime.Descriptors;
+using Jint.Runtime.Interop;
+
+namespace Jint;
+
+/// <summary>
+/// Provides the standard $262 test harness object for ECMAScript Test262 conformance testing.
+/// See https://github.com/tc39/test262/blob/main/INTERPRETING.md
+/// </summary>
+public static class Test262Object
+{
+    /// <summary>
+    /// Installs the $262 object into the engine's global scope and returns it.
+    /// Callers may add additional properties (e.g. <c>agent</c>) to the returned object.
+    /// </summary>
+    public static ObjectInstance Install(Engine engine)
+    {
+        var o = engine.Realm.Intrinsics.Object.Construct(Arguments.Empty);
+
+        o.FastSetProperty("evalScript", new PropertyDescriptor(new ClrFunction(engine, "evalScript",
+            (_, args) =>
+            {
+                if (args.Length > 1)
+                {
+                    throw new ArgumentException("only script parsing supported", nameof(args));
+                }
+
+                var script = Engine.PrepareScript(args.At(0).AsString(), options: new ScriptPreparationOptions
+                {
+                    ParsingOptions = ScriptParsingOptions.Default with { Tolerant = false },
+                });
+
+                return engine.Evaluate(script);
+            }), true, true, true));
+
+        o.FastSetProperty("createRealm", new PropertyDescriptor(new ClrFunction(engine, "createRealm",
+            (_, _) =>
+            {
+                var realm = engine._host.CreateRealm();
+                realm.GlobalObject.Set("global", realm.GlobalObject);
+                return realm.GlobalObject;
+            }), true, true, true));
+
+        o.FastSetProperty("detachArrayBuffer", new PropertyDescriptor(new ClrFunction(engine, "detachArrayBuffer",
+            (_, args) =>
+            {
+                var buffer = (JsArrayBuffer) args.At(0);
+                buffer.DetachArrayBuffer();
+                return JsValue.Undefined;
+            }), true, true, true));
+
+        o.FastSetProperty("gc", new PropertyDescriptor(new ClrFunction(engine, "gc",
+            (_, _) =>
+            {
+                GC.Collect();
+                GC.WaitForPendingFinalizers();
+                return JsValue.Undefined;
+            }), true, true, true));
+
+        o.FastSetProperty("IsHTMLDDA", new PropertyDescriptor(new IsHTMLDDA(engine), true, true, true));
+
+        engine.SetValue("$262", o);
+        return o;
+    }
+}
